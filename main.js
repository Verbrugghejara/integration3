// const mapSection = document.querySelector(".map-section");

// // Stel de nieuwe cursor in (gebruik je eigen afbeelding voor de cursor)
// const customCursorUrl = "/public/assets/magnifyGlass.png";

// mapSection.addEventListener("mouseenter", () => {
//   document.body.style.cursor = `url(${customCursorUrl}), auto`;
// });

// mapSection.addEventListener("mouseleave", () => {
//   document.body.style.cursor = "default";
// });